# Flask-GUI
Flask Implementation of our frontend

### Note: This is an initial commit. More changes are yet to come. For more information on the commit, please look at the Release Notes / Changelog in the commit. 

## Setup
`pip install -r requirements.txt`

## Usage
`python app.py`

Open `127.0.0.1:5000/` on any browser. 